import { FrontDoor } from "@/components/FrontDoor";

/**
 * The landing surface, and the only dark one in the product. One line, a START
 * press, and the sign-in it reveals. It says nothing about what the product does
 * — the mystery is deliberate. Everything past sign-in is the white world.
 */
export default function Landing() {
  return <FrontDoor />;
}
